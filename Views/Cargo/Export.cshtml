@model List<CORE.BackOffice.UI.ViewModels.Cargo.CargoViewModel>

    @{
        Layout = "~/Views/Shared/_LayoutExport.cshtml";
        ViewBag.Title = "ExportToExcel";
    }
    
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.First().IdCargo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.First().Nome)
            </th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.IdCargo)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Nome)
                </td>
            </tr>
        }

    </table>
