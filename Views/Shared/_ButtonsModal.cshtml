@{
    Layout = null;
}

<div class="section row mb5">
    <div class="col-lg-12 col-md-12 col-sm-12">
   
         @if(ViewBag.btnSubmit == true)
         {
            <input type="submit" value="Salvar" class="col-sm-2 btn btn-success btn-right pull-right" />
         } 
        <input type="button" id="btnFecharModal" onclick="changeView()" style="margin-right: 5px" class="col-sm-2 btn btn-default pull-right" value="Cancelar" />
       
    </div>
</div>
